# KIMI CLAW v2 使用说明书

**版本**: 2.0.0  
**发布日期**: 2026-04-01  
**作者**: 明夷 (MoYin Claw)

---

## 📖 目录

1. [产品概述](#产品概述)
2. [安装部署](#安装部署)
3. [快速入门](#快速入门)
4. [核心功能](#核心功能)
5. [配置指南](#配置指南)
6. [最佳实践](#最佳实践)
7. [故障排查](#故障排查)

---

## 产品概述

KIMI CLAW v2 是一个完整的 **AI Agent 系统**，受 Claude Code 架构启发，支持多轮推理、MCP 协议和多 Agent 协作。

### 核心特性

| 特性 | 说明 |
|:---|:---|
| 🤖 **Agent 循环** | 思考→决策→执行→观察的自主循环 |
| 🧠 **智能压缩** | 4 种上下文压缩策略，高效管理长对话 |
| 🔌 **MCP 协议** | 符合 Model Context Protocol 标准 |
| 👥 **多 Agent** | 支持并行、顺序、管道、层级协作 |
| 🔐 **权限系统** | 5 级权限模型，安全可靠 |
| 💾 **会话管理** | 支持分叉(fork)和恢复(continue) |

### 系统架构

```
┌─────────────────────────────────────────────────────────┐
│                    KIMI CLAW v2                        │
├─────────────────────────────────────────────────────────┤
│  Agent 循环 → 思考 → 决策 → 执行 → 观察                │
├─────────────────────────────────────────────────────────┤
│  MCP 服务端 ←→ 外部 MCP 客户端                          │
├─────────────────────────────────────────────────────────┤
│  多 Agent 协调器                                        │
│  ├─ 研究 Agent                                         │
│  ├─ 编码 Agent                                         │
│  └─ 审查 Agent                                         │
├─────────────────────────────────────────────────────────┤
│  工具注册中心 (6 个内置工具)                            │
├─────────────────────────────────────────────────────────┤
│  会话持久化 (JSONL) + CLAUDE.md 加载                    │
└─────────────────────────────────────────────────────────┘
```

---

## 安装部署

### 环境要求

- **Node.js**: >= 18.0.0
- **TypeScript**: >= 5.0.0
- **内存**: 建议 4GB+

### 安装步骤

```bash
# 1. 克隆仓库
git clone https://github.com/moyin/kimi-claw-v2.git
cd kimi-claw-v2

# 2. 安装依赖
npm install

# 3. 编译构建
npm run build

# 4. 运行测试
npm test
```

### 目录结构

```
kimi-claw-v2/
├── src/
│   ├── core/           # 核心模块
│   ├── tools/          # 内置工具
│   └── index.ts        # 入口文件
├── dist/               # 编译输出
├── test/               # 测试文件
├── docs/               # 文档
├── README.md
├── CHANGELOG.md
└── package.json
```

---

## 快速入门

### 基础使用

```typescript
import { KIMI_CLAW } from 'kimi-claw-v2';

// 初始化
const kimi = new KIMI_CLAW();

// 执行单个工具
const result = await kimi.execute('WebSearchTool', {
  query: '最新 AI 新闻'
});

console.log(result);
```

### Agent 模式

```typescript
const kimi = new KIMI_CLAW({ enableAgentLoop: true });

// 运行 Agent - 自动多轮推理
const state = await kimi.runAgent(
  '搜索 Claude Code 最新特性并总结关键变化'
);

console.log(`完成！共 ${state.steps.length} 步`);

// 监听执行过程
kimi.setAgentHandlers({
  onStep: (step) => {
    console.log(`[${step.type}] ${step.content.substring(0, 50)}...`);
  },
  onComplete: (state) => {
    console.log('✅ Agent 执行成功！');
  },
  onError: (error) => {
    console.error('❌ 错误:', error.message);
  }
});
```

---

## 核心功能

### 1. Agent 循环 (Agent Loop)

Agent 循环是系统的核心，实现自主决策：

```
用户 Query
    ↓
思考 (Thought)      → 分析需求
    ↓
决策 (Decide)       → 选择工具/动作
    ↓
执行 (Execute)      → 运行工具
    ↓
观察 (Observe)      → 获取结果
    ↓
循环/完成
    ↓
最终答案
```

**配置选项**:

```typescript
const kimi = new KIMI_CLAW({
  enableAgentLoop: true,
  agentConfig: {
    maxSteps: 10,              // 最大推理步数
    maxTokens: 8000,           // 上下文窗口
    autoApproveTools: ['WebSearchTool'],  // 自动批准的工具
    requireConfirmation: true, // 需要用户确认
    stopOnError: true          // 错误时停止
  }
});
```

### 2. 上下文压缩

当对话过长时，自动压缩上下文：

```typescript
// 手动管理上下文
const compressed = kimi.manageContext(agentSteps);

// 配置压缩策略
const kimi = new KIMI_CLAW({
  agentConfig: {
    compressionStrategy: 'sliding_window',  // 或 'selective', 'summarize', 'hierarchical'
    targetRatio: 0.5                        // 压缩目标比例
  }
});
```

**4 种压缩策略**:

| 策略 | 说明 | 适用场景 |
|:---|:---|:---|
| sliding_window | 保留最近 N 条消息 | 通用场景 |
| selective | 按重要性评分保留 | 关键步骤多 |
| summarize | 摘要化旧消息 | 需要历史上下文 |
| hierarchical | 分层摘要 | 超长对话 |

### 3. MCP 协议支持

将工具暴露为 MCP 接口，与其他 MCP 客户端互通：

```typescript
// 启用 MCP
const kimi = new KIMI_CLAW({ enableMCP: true });

// 处理 MCP 请求
const response = await kimi.handleMCPRequest({
  jsonrpc: '2.0',
  id: 1,
  method: 'tools/list'  // 或 'tools/call', 'server/info', 'server/health'
});
```

**MCP 请求示例**:

```typescript
// 列出可用工具
{
  jsonrpc: '2.0',
  id: 1,
  method: 'tools/list'
}

// 调用工具
{
  jsonrpc: '2.0',
  id: 2,
  method: 'tools/call',
  params: {
    name: 'WebSearchTool',
    arguments: { query: 'AI 新闻' }
  }
}
```

### 4. 多 Agent 协作

将复杂任务分解给多个专业 Agent 并行处理：

```typescript
// 启用多 Agent
const kimi = new KIMI_CLAW({ enableMultiAgent: true });

// 注册多个 Agent
kimi.registerAgent({
  id: 'researcher',
  name: 'Research Agent',
  role: 'researcher',
  description: '擅长搜索和信息收集',
  capabilities: ['search', 'summarize']
});

kimi.registerAgent({
  id: 'coder',
  name: 'Code Agent',
  role: 'developer',
  description: '擅长代码生成和调试',
  capabilities: ['code', 'debug']
});

// 分配任务
const task1 = kimi.assignTask({
  agentId: 'researcher',
  task: '研究 2024 年最佳 AI 框架',
  priority: 'high'
});

const task2 = kimi.assignTask({
  agentId: 'coder',
  task: '创建示例实现',
  priority: 'medium',
  dependencies: [task1.taskId]  // 依赖 task1
});

// 执行（并行）
const results = await kimi.executeTasks(
  [task1.taskId, task2.taskId],
  'parallel'  // 或 'sequential', 'pipeline', 'hierarchical'
);
```

**4 种协作策略**:

| 策略 | 说明 | 适用场景 |
|:---|:---|:---|
| sequential | 顺序执行 | 有依赖关系的任务 |
| parallel | 并行执行 | 独立任务，追求速度 |
| pipeline | 管道模式 | 数据流处理，A 输出→B 输入 |
| hierarchical | 层级模式 | Manager-Worker 结构 |

**Agent 间通信**:

```typescript
// 发送消息
kimi.getMultiAgentCoordinator()?.sendMessage({
  from: 'researcher',
  to: 'coder',
  type: 'request',
  content: '这是 API 文档...'
});

// 广播
kimi.getMultiAgentCoordinator()?.broadcast(
  'manager',
  '所有 Agent 开始工作！'
);
```

### 5. 会话管理

```typescript
// 分叉会话（创建分支）
const forked = kimi.forkSession(5);  // 在第 5 条消息处分叉

// 恢复历史会话
kimi.continueSession('20260401-abcd');

// 列出所有会话
const sessions = kimi.listSessions();

// 获取当前会话
const current = kimi.getCurrentSession();
```

---

## 配置指南

### 权限配置

配置文件位置: `~/.kimi-claw/permissions.json`

```json
{
  "version": "1.0.0",
  "rules": {
    "alwaysAllow": ["WebSearchTool", "FileReadTool"],
    "alwaysAsk": ["BashTool", "AgentTool", "FileWriteTool"],
    "alwaysDeny": []
  },
  "settings": {
    "defaultPermissionLevel": "ask",
    "logAllExecutions": true
  }
}
```

**权限级别**:

| 级别 | 说明 | 示例工具 |
|:---|:---|:---|
| none | 无权限 | - |
| low | 只读操作 | WebSearchTool, FileReadTool |
| medium | 可能修改状态 | FileWriteTool, MessageTool |
| high | 危险操作 | BashTool, AgentTool |
| critical | 系统级操作 | - |

### CLAUDE.md 配置

在项目目录创建 `CLAUDE.md` 文件，自动加载为上下文：

```markdown
---
title: 项目名称
description: 项目描述
version: 1.0.0
---

# 项目上下文

## 架构
...

## 规范
...
```

支持层级继承：子目录的 CLAUDE.md 会覆盖父目录的配置。

### 完整配置示例

```typescript
const kimi = new KIMI_CLAW({
  // 基础配置
  sessionId: 'my-session',
  userId: 'user-123',
  
  // 路径配置
  configDir: '~/.kimi-claw',
  sessionsDir: '~/.kimi-claw/sessions',
  workspaceRoot: './my-project',
  
  // 功能开关
  autoLoadCLAUDE: true,
  enableAgentLoop: true,
  enableMCP: true,
  enableMultiAgent: true,
  
  // Agent 配置
  agentConfig: {
    maxSteps: 10,
    maxTokens: 8000,
    autoApproveTools: ['WebSearchTool', 'FileReadTool'],
    requireConfirmation: true,
    stopOnError: true,
    compressionStrategy: 'sliding_window',
    targetRatio: 0.5
  }
});
```

---

## 最佳实践

### 1. Agent 使用建议

```typescript
// ✅ 好的做法：清晰的任务描述
const state = await kimi.runAgent(
  '搜索 React 19 的新特性，整理成表格，包含特性名称、说明和代码示例'
);

// ❌ 避免：过于宽泛的任务
const state = await kimi.runAgent('告诉我关于 React 的一切');
```

### 2. 权限管理建议

```typescript
// 生产环境：严格权限
const kimi = new KIMI_CLAW({
  agentConfig: {
    autoApproveTools: ['WebSearchTool'],  // 只自动批准安全工具
    requireConfirmation: true              // 其他都需要确认
  }
});

// 开发环境：宽松权限
const kimi = new KIMI_CLAW({
  agentConfig: {
    autoApproveTools: ['WebSearchTool', 'FileReadTool', 'FileWriteTool'],
    requireConfirmation: false
  }
});
```

### 3. 多 Agent 任务分解

```typescript
// 复杂任务分解示例：开发一个功能
const kimi = new KIMI_CLAW({ enableMultiAgent: true });

// 1. 研究
const research = kimi.assignTask({
  agentId: 'researcher',
  task: '调研最佳实践',
  priority: 'high'
});

// 2. 设计（依赖研究）
const design = kimi.assignTask({
  agentId: 'designer',
  task: '设计架构',
  priority: 'high',
  dependencies: [research.taskId]
});

// 3. 编码（依赖设计）
const code = kimi.assignTask({
  agentId: 'coder',
  task: '实现功能',
  priority: 'high',
  dependencies: [design.taskId]
});

// 4. 测试（可并行）
const test = kimi.assignTask({
  agentId: 'tester',
  task: '编写测试',
  priority: 'medium',
  dependencies: [code.taskId]
});

// 顺序执行（有依赖关系）
await kimi.executeTasks(
  [research.taskId, design.taskId, code.taskId, test.taskId],
  'sequential'
);
```

### 4. 会话管理建议

```typescript
// 长任务定期分叉，保留检查点
const state = await kimi.runAgent('长时间任务...');

// 每完成一个重要阶段，创建分叉
const checkpoint = kimi.forkSession();

// 如果后续出错，可以回到检查点
kimi.continueSession(checkpoint.metadata.id);
```

---

## 故障排查

### 常见问题

#### 1. Agent 循环不停止

**现象**: Agent 一直运行，没有输出结果

**解决**:
```typescript
const kimi = new KIMI_CLAW({
  agentConfig: {
    maxSteps: 10,      // 限制最大步数
    maxTokens: 8000    // 限制上下文大小
  }
});

// 手动停止
kimi.stopAgent();
```

#### 2. 上下文超限

**现象**: 报错 "Context length exceeded"

**解决**:
```typescript
const kimi = new KIMI_CLAW({
  agentConfig: {
    compressionStrategy: 'summarize',  // 启用压缩
    targetRatio: 0.5                    // 压缩到 50%
  }
});
```

#### 3. 权限被拒绝

**现象**: 工具执行报错 "Permission denied"

**解决**:
```typescript
// 检查权限
console.log(kimi.isToolAllowed('BashTool'));

// 允许工具
kimi.allowTool('BashTool');

// 或修改配置文件 ~/.kimi-claw/permissions.json
```

#### 4. MCP 连接失败

**现象**: MCP 请求无响应

**解决**:
```typescript
// 检查 MCP 是否启用
const kimi = new KIMI_CLAW({ enableMCP: true });

// 验证服务器状态
const health = await kimi.handleMCPRequest({
  jsonrpc: '2.0',
  id: 1,
  method: 'server/health'
});
```

#### 5. 多 Agent 任务卡住

**现象**: 任务一直显示 running 状态

**解决**:
```typescript
// 设置超时
const kimi = new KIMI_CLAW({
  multiAgentConfig: {
    taskTimeout: 300000  // 5 分钟超时
  }
});

// 查看运行中的任务
const running = kimi.getMultiAgentCoordinator()?.getRunningTasks();
console.log('运行中任务:', running);
```

### 调试技巧

```typescript
// 启用详细日志
kimi.setAgentHandlers({
  onStep: (step) => {
    console.log(`[${step.type}]`, step);
  }
});

// 获取 Agent 状态
const state = kimi.getAgentState();
console.log('当前状态:', state);

// 获取执行历史
const history = kimi.getHistory();
console.log('执行历史:', history);
```

---

## API 参考

### KIMI_CLAW 类

#### 构造函数

```typescript
new KIMI_CLAW(config?: KIMI_CLAW_Config)
```

#### 方法

| 方法 | 说明 | 返回值 |
|:---|:---|:---|
| `execute(tool, input)` | 执行单个工具 | ExecutionResult |
| `runAgent(query)` | 运行 Agent 循环 | AgentState |
| `stopAgent()` | 停止 Agent | void |
| `isAgentRunning()` | 检查 Agent 状态 | boolean |
| `getAgentState()` | 获取 Agent 状态 | AgentState \| undefined |
| `setAgentHandlers(handlers)` | 设置事件监听 | void |
| `registerAgent(agent)` | 注册 Agent | void |
| `assignTask(task)` | 分配任务 | TaskAssignment |
| `executeTasks(ids, strategy)` | 执行任务 | TaskResult[] |
| `handleMCPRequest(req)` | 处理 MCP 请求 | MCPResponse |
| `forkSession(index?)` | 分叉会话 | Session \| null |
| `continueSession(id)` | 恢复会话 | Session \| null |
| `listSessions(options?)` | 列会话 | SessionIndexEntry[] |
| `listTools()` | 列工具 | ToolDescription[] |
| `allowTool(name)` | 允许工具 | void |
| `denyTool(name)` | 拒绝工具 | void |

---

## 附录

### 内置工具清单

| 工具名 | 类别 | 权限 | 说明 |
|:---|:---|:---:|:---|
| WebSearchTool | SEARCH | low | 网页搜索 |
| FileReadTool | FILE | low | 读取文件 |
| FileWriteTool | FILE | medium | 写入文件 |
| AgentTool | AGENT | high | 生成子 Agent |
| BashTool | EXEC | high | 执行命令 |
| MessageTool | MESSAGE | medium | 发送消息 |

### 版本历史

| 版本 | 日期 | 说明 |
|:---|:---|:---|
| 2.0.0 | 2026-04-01 | 完整发布，100% 对齐 Claude Code |

### 获取帮助

- **GitHub Issues**: https://github.com/moyin/kimi-claw-v2/issues
- **文档**: https://github.com/moyin/kimi-claw-v2#readme
- **更新日志**: [CHANGELOG.md](./CHANGELOG.md)

---

**版权**: MIT License  
**作者**: 明夷 (MoYin Claw)  
**致谢**: 受 Claude Code 架构启发

---

> 《道德经》云：「千里之行，始于足下。」
> 
> KIMI CLAW v2，为您的 AI 之旅助力。
